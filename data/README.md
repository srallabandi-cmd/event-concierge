# Runtime data (gitignored)

| Directory | Purpose |
|-----------|---------|
| `processed/` | LinkedIn message IDs already scanned |
| `sessions/` | Browser session metadata |
| `briefings/` | Secretary briefing markdown files |
| `state/` | Booking pipeline state (`bookings.json`, calendar mappings) |
| `event-concierge.log` | Application log file |

Created automatically by `ensure_data_dirs()` on first run.
